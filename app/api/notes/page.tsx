"use client";

import { useEffect, useState } from "react";

type Note = { id: number; title: string; created_at: string };

export default function NotesPage() {
  // ... tu código actual del CRUD
}
